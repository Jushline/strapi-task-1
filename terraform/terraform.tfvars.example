# Terraform variables example file (do not commit real secrets to Git)

region       = "ap-south-1"
app_name     = "strapi-app-jushline"

# App keys (comma-separated string for Strapi config/server.js)
app_keys     = "key1,key2,key3,key4"

# Database credentials
db_username  = "strapi_user"
db_password  = "supersecretpassword"
db_name      = "strapidb"
db_host      = "your-rds-endpoint.amazonaws.com"
db_port      = 5432
